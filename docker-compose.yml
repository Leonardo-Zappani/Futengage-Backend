version: '3.2'
services:
  FutEngage-mail:
    image: 'sj26/mailcatcher'
    ports:
      - "1080:1080"
      - "1025:1025"

  FutEngage-redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  FutEngage-db:
    image: postgres:14-alpine
    environment:
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
    volumes:
      - "futengage-db-data:/var/lib/postgresql/data"

volumes:
  futengage-db-data: