= turbo_stream.update :attachment_list, partial: 'attachments/attachment', collection: @record.attachments, as: :attachment
= turbo_stream.replace dom_id(@record), partial: 'transactions/transaction', locals: { transaction: @record }
= turbo_stream.update :flash, render(FlashComponent.new(flash: flash))
