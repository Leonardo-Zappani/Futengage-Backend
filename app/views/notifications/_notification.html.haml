= link_to notification.url, method: :get, class: 'c-dropdown__item flex items-center space-x-2', data: { turbo_frame: :drawer } do
  .block.truncate
    .paragraph-base= notification.title
    .paragraph-muted= notification.message