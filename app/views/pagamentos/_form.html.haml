= form_for @pagamento do |f|
  - if @pagamento.errors.any?
    = render 'errors', errors: @pagamento.errors
  .flex.flex-col.space-y-4.sm:space-y-6
    .form-group
      = f.submit class: "btn btn--primary"