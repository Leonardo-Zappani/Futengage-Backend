= form_for @history do |f|
  - if @history.errors.any?
    = render 'errors', errors: @history.errors
  .flex.flex-col.space-y-4.sm:space-y-6
    .form-group
      = f.submit class: "btn btn--primary"